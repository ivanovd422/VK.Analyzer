task installGitHook(type: Copy) {
    from new File("$rootProject.rootDir/ktlint", 'pre-commit')
    into { new File(rootProject.rootDir, '.git/hooks') }
    fileMode 0777
}


task preCommitCheck {
    dependsOn 'app:ktlint'
}

tasks.getByPath(':app:preBuild').dependsOn installGitHook